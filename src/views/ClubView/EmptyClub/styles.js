import {StyleSheet} from 'react-native';
import { height, width } from '../../../constants/sizes';
import {
  COLOR_PRIMARY,
  WHITE,
  INCOME_BACKGROUND,
  VIOLET_TEXT,
} from '../../../themes/colors';
import Fonts from '../../../themes/Fonts';

export default StyleSheet.create({
  container: {
   height:height*637/896,
   width: width*382/414,
  },
 
});
