import {StyleSheet} from 'react-native'
import {WHITE} from '../../themes/colors'
import font from '../../themes/Fonts'

export default StyleSheet.create({
    input:{
        borderWidth:2,
        borderColor:WHITE,
        borderRadius:15,
        paddingHorizontal:24,
        fontFamily:font.playBold,
        fontSize:14,
        height:40
    }
}) 

