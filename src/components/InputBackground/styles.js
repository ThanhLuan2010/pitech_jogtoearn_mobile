import {StyleSheet} from 'react-native'
import { height } from '../../constants/sizes'
import {WHITE} from '../../themes/colors'
import Fonts from '../../themes/Fonts'

export default StyleSheet.create({
    container:{
        height: height*28.88/896+5,  
    },
  
}) 

